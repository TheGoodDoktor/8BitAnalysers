TODO FOR FIRST RELEASE
- eliminate all dupe bank bugs (some still exist)
- OnEnterEditMode() get working
- edit mode doesnt restore properly. emulation bugs afer restoring
- get graphics viewer displaying sprites
- remove dupe banks from graphics viewer dropdown. also unused banks
- tidy up sprites viewer detail pane
- bank addresses should be offsets (including function labels)
- fix trace button in code analysis view (sometimes works)
- remove validatorpath from global config in release builds
- remove BIOS checkbox on globals view for hucard games

TODO
- operand dropdowns dont work
- get Word data item set to display as Pointer to work when outputting assembly
- add audio viewer: disable channels etc
- create new graphics viewer that removes all the stuff I dont need. off-screen buffers etc
- in graphics viewer, label which palette is currently selected 
- change Current palette option in graphics viewer. doesnt make sense for PCE
- Show register value on tooltips
- tidy tooltips for PCE related instructions
- make bank label be an offset instead of address. add toggle in UI.
- display vram transfer address in vram viewer. MAWR. maybe with indicator
- in graphics viewer, label which palette is currently selected
- get 3bpp, 2bpp sprite working in graphics viewer. 
- tooltips on pce registers describing what they do
- in sprites viewer make the Results tab a Search tab and move find all into it?
- change Current palette option in graphics viewer. doesnt make sense for PCE
- local labels asm export
- if FBankSet::GetMappedAddressFromUsage() doesnt have enough usage fallback to other means?
- hookup RegisterCodeExecutedHuC6280
- reset viewers on soft reset
- dupe bank issues in hello.pce project. "FAddressRef::SetVal on unmapped bank 62 ROM_01_3. ref will be invalid." followed by assert in FItemRefernceTracker::RegisterAccess
- deal with multiple operands, such as block operations etc [partially done]
- hook up more events: audio, etc
- improve Claude.md
- tidy layout of background viewer. clunky
- history sprite palettes are not correct sometimes. rabio lepus intro.
- add a way to display or goto the ROM address (in the cart's 2mb address space)
- show bitmap format in code analysis view when cursor is on PCE sprite/BG
- add optional label when formatting sprites in sprite viewer
- get claude to see if CheckPointerRefInstructionHuC6280 needs to be implemented
- get claude to write unit test for GetInstructionAddressModeHuC6280 to compare against huc6280dasm_op
- BBR0 instruction displayed wrong in code analysis. doesnt show ptr to zp memory. sometimes works.
- add PCE palette display type.
- get character map view working
- absolute X & Y instructions dont display absolute value in code analysis view. see LDY NNNN,X LDA NNNN,X CMP NNNN,Y (opcodes BC, BD & D9)
- fix system vectors breaking asm output
- auto-wrap multi-line comments
- add grayscale palette so we can view vram tiles & sprites when no palettes are set. same for Screen view and Background view? see Mesen
 
BUGS
- figure out why some functions show up as data. see hero tonma data_wram_3bf2
- vram seems to display sprite 63 pattern data at the start of vram.
- TIA instruction lost it's self modifying code "Operand Data"  
- asm export fails if you export right after reset, when UNUSED banks are mapped.
- empty JMP address in disassembly that causes the asm exporter to think the jmp destination is address 0. saw it on rabio lepus at location e019 after loading project.
- when hovering over first operand of TIA instruction that is a label, the middle operand disappears.
- If you do Reset via the option menu it loses all the labels we added in FPCEEmu::LoadProject() (entry point and HW labels)
- sprites formatted by sprite viewer dont draw correctly.
- Exception thrown at 0x00007FFF2C77AB5E (atio6axx.dll) in PCEAnalyser.exe: 0xC0000005: Access violation reading location 0xFFFFFFFFFFFFFFFF. started happening when including mcp server. 
- palettes get added at index 67
- code snippet tooltip shows all FF for opcode values sometimes

IDEAS
- press delete to remove formatting
- label IRQ in callstack
- add way to understand/visualise if interrupts are active?
- make screen appear when loading a saved project
- add "view in memory viewer" in code analysis view
- minimap on scrollbar
- copy instruction tooltip text from mesen?
- show r/w colouring in memory viewer
- have different sprite display modes for when sprites are packed or bitplane reduced.
- when capturing palette for history sprite, capture after some time has elpased.
- [perf] Create code analysis pages on demand. would make creating banks trivial
- [perf] reserve bank id instead of creating all the banks up front.
- when hovering over PCE memory mapped registers, give a description what they do. or add comments for the memory location
- lazily create dupe banks. it's possible now we redirect code analysis to primary bank. reduces banks from 700->150 banks 
- override assert with our own that lets you recover
- create dupe banks lazily. only save state of the primary ones?
- automatically mark memory as graphics when it gets transferred to vram
- show unexplored bytes as array
- preview format before format is pressed
- make graphics viewer texture smaller when viewing banks. it's too big for 8k banks.
- add button to save all sprites in history view to png
- always collapse/hide bytes in HW page.
- add scroll overlay on background view (like Mesen does)
- ask claude to look at Mesen "draw partial frame" option to see how it does it.

DONE
- perf of sprites results tab is really bad. improved but DrawAddressLabel is expensive when called > once
- STA zero page shows HW page label in code analysis view. opcode 85. 
- IRQ vector has wrong label. hero tonma IRQ1 vector is in ROM 1 but has label func_ROM_00_433A_IRQ1Vector
- add ctrl + up/down to keyboard shortcuts display
- crash in ImGui::DockContextEndFrame(). can happen on start up.
- in sprites viewer make the Results tab a Search tab and move find all into it?
- results view in sprite viewer looks shit. thumbnails are too small.
- add read_bank mcp tool, to read bank not mapped in.
- show which palettes are BG/sprites/user in palette dropdowns
- get asm export for memory range working again
- Restore asm export for physical memory
- sprites dont display correctly in code analysis view correctly. hero tonma clouds.
- add memory viewer (at least for vram). see https://github.com/ocornut/imgui_club
- get BG data item working
- add suport for <label> + <offset> to asm exporter?
- fix first code byte always being data
- dont display dupe banks in banks mapped view 
- BRA instruction doesn't show label or branch lines
- fail to load cd game if no bios was loaded
- If you do Reset via the option menu it loses all the labels we added in FPCEEmu::LoadProject() (entry point and HW labels)
- Allow you to edit register values.
- add keyboard shortcut for changing font size
- when formatting memory as sprite dont use current (transient) palette data. 
- fix code analysis detail pane getting stuck off screen when fixed
- AND (zp) [opcode $32] doesnt show zp ptr in code analysis view.
- EOR (zp) [opcode $52] doesnt show zp ptr in code analysis view.
- ADC (zp) [opcode $72] doesnt show zp ptr in code analysis view.
- STA (zp) [opcode $92] doesnt show zp ptr in code analysis view.
- LDA (zp) [opcode $B2] doesnt show zp ptr in code analysis view.
- CMP (zp) [opcode $D2] doesnt show zp ptr in code analysis view.
- SBC (zp) [opcode $F2] doesnt show zp ptr in code analysis view.
- RMBi doesnt show zp ptr in code analysis view.
- add mcp tool to reset emulation
- make JMP (XXXX) create a label at the jump target. currently it doesn't create a label.
- Implement CodeAnalyserHuC6280.cpp
- code analysis view doesnt go to address that was last viewed after loading project.
- dont add cd bios labels in WRAM for joy/joyena etc
- make MCP Rationale text accessible (or make button to add as comment)